# Evaluation Frameworks (chỉ dùng khi benchmark offline)
deepeval>=0.21.0
ragas>=0.1.0
prometheus-eval>=0.1.0

# Vector Database (chỉ dùng khi cần semantic search)
pymilvus>=2.3.0
pinecone-client>=3.0.0

# ELK Stack (chỉ dùng khi cần centralized logging)
elasticsearch>=8.11.0
# Note: Logstash is a Java app, not a pip package.
# For Python -> Logstash integration use: python-logstash-async>=2.5.0

# Additional utilities for evaluation
datasets>=2.18.0
scikit-learn>=1.3.0
numpy>=1.24.0